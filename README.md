# Setting up the dev env

1. Install node and npm
2. cd `swing-dance-moves-generator`
3. npm install


# Common actions

Starting the dev server:

```sh
npm run dev
```

Linting:

```sh
npm run lint
```

Building:

```sh
npm run build
```

Deploying:

```sh
npm run deploy
```
